import { ContextDataInitial as InitialContext } from "./ctx.js";
export interface Page {
    src: string;
    id: string;
    md: string;
    data: PageData;
    url: string;
    inputKey: string;
}
export interface PageData {
    title: string;
    description?: string;
    keywords?: string[];
    meta?: Record<string, string>;
}
export type PageOut = Pick<Page, "data" | "url">;
export declare function pageOut({ data, url }: Page): PageOut;
export declare function getPages(paths: string[], root: string, ctx: InitialContext): Promise<Record<string, Page>>;
export declare function buildPage(path: string, root: string): Promise<Page>;
export declare function refreshPage(page: Page, root: string): Promise<Page>;
//# sourceMappingURL=page.d.ts.map